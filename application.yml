eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka
    fetch-registry: 'true'
    register-with-eureka: 'true'
  instance:
    hostname: localhost
    prefer-ip-address: 'true'
